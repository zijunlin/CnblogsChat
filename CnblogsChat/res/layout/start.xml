<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="@dimen/imgLogoWidth"
        android:layout_height="@dimen/imgLogoHeight"
        android:layout_centerInParent="true"
        android:scaleType="fitXY"
        android:src="@drawable/logo_small" />

    <ProgressBar
        android:id="@+id/pgbInit"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/imgLogo"
        android:layout_below="@id/imgLogo"
        android:layout_marginTop="30dp" />

    <TextView
        android:id="@+id/txtMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/pgbInit"
        android:layout_toRightOf="@id/pgbInit" 
        android:layout_marginLeft="10dp">
    </TextView>

</RelativeLayout>